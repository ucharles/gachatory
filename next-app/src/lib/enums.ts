export enum perPageEnum {
  SMALL = 20,
  MEDIUM = 40,
  LARGE = 60,
}

export enum cacheTimeEnum {
  ONE_MINUTE = 60,
  FIVE_MINUTES = 300,
  TEN_MINUTES = 600,
  FIFTEEN_MINUTES = 900,
}

export enum capsuleTagPropertyEnum {
  TITLE = 1,
  CHARACTER = 2,
  BRAND = 3,
  SERIES = 4,
  AUTHOR = 5,
  CATEGORY = 6,
  ELEMENT = 7,
}
